{
  "name": "best-japanese-food",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build:data": "pnpm --filter @bff/build-data run data",
    "test": "pnpm -r --if-present test",
    "lint": "pnpm -r --if-present lint",
    "build": "pnpm build:data && pnpm -r build",
    "build:apps": "pnpm -r build"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.0",
    "oxlint": "^1.75.0",
    "typescript": "~6.0.2",
    "vite": "^8.2.0",
    "vitest": "^4.0.0"
  }
}
